# 验证方案

> 实施各阶段完成后的验证步骤、命令和预期结果。

---

## 1. 冒烟测试（每步完成后立即执行）

### 1.1 现有测试不回归

```bash
cd /home/gwxie/master_work-tmp/AUV_Master_Project
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -m pytest tests/ -v
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -m pytest brain_linux/src/auv_bridge/test/ -v
```

**预期**: 全部 14 个现有测试通过（2 contract + 2 debug + 6 arbiter + 4 guard）。

### 1.2 新模块可导入

```bash
cd /home/gwxie/master_work-tmp/AUV_Master_Project
/usr/bin/python3 -c "
from sim_holoocean.interfaces.mock_amd_delay import TransportDelayQueue
from sim_holoocean.interfaces.mock_amd_sensor_cache import SensorSampleCache, SensorSnapshot
from sim_holoocean.interfaces.mock_amd_chaos import ChaosProfile, ChaosInjector
print('All new modules import OK')
"
```

**预期**: 无 ImportError。

### 1.3 Mock AMD 启动

```bash
cd /home/gwxie/master_work-tmp/AUV_Master_Project
/usr/bin/python3 -c "
from sim_holoocean.interfaces.mock_amd_server import MockAmdUdpServer
import yaml
with open('config/bridge_params.protocol_udp.yaml') as f:
    cfg = yaml.safe_load(f)
# 验证 mock_amd 节被正确读取
mock = cfg.get('mock_amd', {})
print(f'mock_amd config loaded: {bool(mock)}')
print(f'chaos enabled: {mock.get(\"chaos\", {}).get(\"enabled\", False)}')
"
```

**预期**: 配置正确加载，无 KeyError。

---

## 2. Phase 1 回归测试

### 2.1 运行全部协议合同测试

```bash
cd /home/gwxie/master_work-tmp/AUV_Master_Project
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -m pytest tests/test_protocol_contract.py -v
```

**预期**: T1.1-T1.9 全部通过。

### 2.2 单个测试运行（调试用）

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -m pytest tests/test_protocol_contract.py::test_downlink_endianness -v
```

### 2.3 往返一致性测试详情

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -m pytest tests/test_protocol_contract.py -v -k "roundtrip"
```

**预期**: T1.7 + T1.8 通过。T1.8 可能因 `KNOWN_DEVIATION_D1` 需要特殊断言逻辑。

---

## 3. Phase 2 硬件行为测试

### 3.1 仲裁器超时测试

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -m pytest brain_linux/src/auv_bridge/test/test_arbiter.py::test_heartbeat_timeout_fallback -v
```

**预期**: 超时后返回 `SAFETY_FALLBACK`。

### 3.2 自治守卫测试

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -m pytest brain_linux/src/auv_bridge/test/test_autonomy_guard.py -v
```

**预期**: 电压/置信度/超时 3 个新增用例通过，原有 4 个用例不回归。

### 3.3 硬件行为测试套件

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -m pytest tests/test_hw_behavior.py -v
```

**预期**: T2.1-T2.8 全部通过。

---

## 4. 混沌注入验证

### 4.1 单元测试

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -m pytest tests/test_mock_amd_chaos.py -v
```

**预期**: 全部通过。

### 4.2 集成测试（手动）

```bash
# Terminal 1: 启动 Mock AMD（带混沌配置）
cd /home/gwxie/master_work-tmp/AUV_Master_Project
# 临时启用混沌
/usr/bin/python3 -c "
import sys
sys.path.insert(0, '.')
from sim_holoocean.interfaces.mock_amd_server import MockAmdUdpServer
from common.protocol import build_downlink_packet
import socket, time, struct

# 手动发送带混沌的上行帧并观察
print('Integration test: chaos injector')
print('This requires a running HoloOcean instance.')
print('Skipping automated integration test.')
"
```

### 4.3 Wireshark 抓包验证

```bash
# 安装 tshark（如果未安装）
sudo apt-get install -y tshark

# 抓取 UDP 52364/52365 端口流量
sudo tshark -i lo -f "udp port 52364 or udp port 52365" -w /tmp/mock_amd_capture.pcapng
```

**验证项**:
- 下行帧长度固定 72 字节
- 上行帧长度固定 145 字节
- 校验和字段（byte 69 / byte 142）正确
- 启用丢包后可见帧间隔不均匀
- 启用棕色断电后可见上行帧缺失周期

---

## 5. 攻击站验证

### 5.1 独立运行

```bash
cd /home/gwxie/master_work-tmp/AUV_Master_Project
/usr/bin/python3 scripts/attacker_station.py --profile heartbeat --duration 10
```

**预期**: 10 秒内发送 10 条心跳，输出 RTT 统计。

### 5.2 CSV 报告检查

```bash
ls -la log/attacker_station_*.csv
head -5 log/attacker_station_*.csv
```

**预期**: CSV 文件存在，包含 timestamp/profile/mode/rtt_ms/response_received 列。

---

## 6. 完整回归

### 6.1 全量测试

```bash
cd /home/gwxie/master_work-tmp/AUV_Master_Project
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -m pytest \
  tests/ \
  brain_linux/src/auv_bridge/test/ \
  -v \
  --tb=short
```

**预期**: 全部测试通过（原有 14 + 新增 ~20 = ~34 个测试）。

### 6.2 覆盖率报告（可选）

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -m pytest \
  tests/test_protocol_contract.py \
  tests/test_hw_behavior.py \
  tests/test_mock_amd_chaos.py \
  --cov=sim_holoocean.interfaces.mock_amd_server \
  --cov=sim_holoocean.interfaces.mock_amd_delay \
  --cov=sim_holoocean.interfaces.mock_amd_sensor_cache \
  --cov=sim_holoocean.interfaces.mock_amd_chaos \
  --cov-report=term-missing
```

**预期目标**: 新增模块行覆盖率 > 80%。

---

## 7. 验证检查清单

| # | 检查项 | 命令/方法 | 通过标准 |
|---|--------|-----------|----------|
| V1 | 现有测试不回归 | `pytest tests/` | 14/14 pass |
| V2 | 新模块可导入 | `python3 -c "import ..."` | 无异常 |
| V3 | 协议合同测试 | `pytest test_protocol_contract.py` | T1.1-T1.9 pass |
| V4 | 硬件行为测试 | `pytest test_hw_behavior.py` | T2.1-T2.8 pass |
| V5 | 仲裁器扩展测试 | `pytest test_arbiter.py` | T2.5 pass, 不回归 |
| V6 | 守卫扩展测试 | `pytest test_autonomy_guard.py` | T2.6-T2.8 pass, 不回归 |
| V7 | 混沌注入测试 | `pytest test_mock_amd_chaos.py` | 全部 pass |
| V8 | 攻击站启动 | `python scripts/attacker_station.py --help` | 无异常 |
| V9 | 配置加载 | 检查 YAML 解析 | mock_amd 节正确读取 |
| V10 | Wireshark 帧长 | tshark 抓包 | 72B / 145B |
| V11 | 校验和正确 | tshark + 手动计算 | byte 69/142 一致 |
| V12 | 全量回归 | `pytest tests/ + brain_linux/...` | 全部 pass |
