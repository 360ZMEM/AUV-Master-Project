# 审核报告_初稿逻辑与架构图一致性_20260806.md

> 触发指令：pass2 与架构图更新同时进行——(1) 按 AGENTS_linux §6.1 二次确认 `研究生毕业设计初稿.md`（2284 行）前后逻辑；(2) 清点所有架构图与 drawio、维护路径、整体审核前后矛盾；(3) 定界（本次不做，纳入计划）：结合 01/02/03 早前 substantial 更新，评估 drawio 是否需补充/正文位置是否需调整。
> 审核侧：macwin（辅助侧）；审核对象为只读，不做正文改动。
> 日期：2026-08-06

---

## 第一部分：`研究生毕业设计初稿.md` 前后逻辑全面审核（Task #1）

### 0. 结论先行
初稿是一份**「真实章节正文 + 大量 AI『第二导师』Q&A 残留」交织**的历史工作底稿，**不是**可直接提交的连续论文。其正文实体只覆盖到第一章（完整）、第二章（完整）、第三章（仅到 3.1/3.2/3.3 片段），**第四、五章无正文实体**（正文实体在 `docs/thesis/paper/04new、05new`）。存在 **3 套互相矛盾的章节组织**、**重复章标题**、**六章/七章自相矛盾**等结构性问题。**结论：初稿仅可作为「素材与思路存档」，不应作为 pass2 的加工主体；pass2 的加工主体应是 `docs/thesis/paper/0X(new).md` 五章。**

### 1. 三套互相矛盾的「章节组织」（最严重逻辑问题）

| 来源 | 章节数 | 结构 | 位置 |
|---|---|---|---|
| **A. 目录（暂拟）** | 6 章 | 1 绪论 / 2 系统设计与建模 / 3 声磁状态估计 / 4 决策与控制 / 5 实验 / (6 小结隐含) | 初稿 `#L5-L100` |
| **B. 第一章 1.5 组织架构正文** | 声称"六章"实列**七章** | 1 绪论 / 2 系统+双脑 / **3 海缆磁场建模+声磁融合反演** / **4 UA-MPC** / **5 行为树决策** / **6 集成仿真海试** / 7 总结 | 初稿 `#L219-L233` |
| **C. paper/ 实体章节** | 5 章 | 1 背景 / 2 系统设计 / 3 声磁状态估计 / 4 决策与控制 / 5 实验 | `docs/thesis/paper/01…05new` |

**矛盾点**：
- **B 明确写"本文共分为六章"（`#L219`），却列出了第一到第七章共 7 个（`#L221-L233`）** —— 自相矛盾。
- **B 把"磁场建模"划入第三章、"UA-MPC"独立为第四章、"行为树"独立为第五章**；而 **C（实体）把磁场建模并入第二章 2.3、把 UA-MPC 与行为树合并为第四章"决策与控制"**。两套划分的知识边界完全不同。
- **A（目录）与 C（实体）大体接近（都是 5~6 章、磁场建模在第二章）**，但 A 把"声呐几何/视觉标定"列为 2.4（`#L40`），实体 C 的 2.4 是"声呐成像几何 + 视觉-磁场协同标定"——名称接近但 A 的 2.3 只提单芯+三相漏磁两小节，实体 C 的 2.3 扩为 5 个小节（2.3.1–2.3.5，含降维反演定理）。

> **判定**：初稿三套组织中，**只有 C（paper 实体）是当前真正在写的版本**。A、B 均为早期设想，已被 substantial 更新超越。**1.5 组织架构（B）与实体（C）严重不一致**，这是最需要注意的前后矛盾——但注意：**实体 paper 的 [01_background 1.5 节](file:///Users/bytedance/coding/AUV-Master-Project/docs/thesis/paper/01_background_and_significance.md#L125-L134) 已是正确的"六章版"**（1 背景/2 系统/3 估计/4 决控/5 实验/6 总结），逻辑自洽。**所以矛盾只存在于初稿内部，paper 实体侧已修正。**

### 2. 重复章标题（结构污染）

第二章标题 **`# 第二章 基于对齐行业标准的 AUV 系统架构与多源感知建模` 出现 4 次**：`#L764`、`#L1086`、`#L1341`、`#L1588`。这是因为初稿把每一轮 Q&A 产出的 2.x 小节各自带一个章标题拼接进来（2.1–2.2 在 L764 段、2.2 硬件表在 L1086 段、2.3 磁场建模在 L1341 段、2.4 标定在 L1588 段）。**同理第一章也在 `#L109` 出现一次后，正文被 Q109/A111 等打断。** 若误把初稿当加工主体，会产生 4 个"第二章"。

### 3. 大面积 AI「第二导师」Q&A 残留（约占全文 55%+）

初稿正文被以下非论文内容反复打断：
- **`## Q109`–`## Q128` / `## A111`–`## A130`**（`#L356` 起，共约 20 组）：AI 问答对话残留。
- **`### 👨‍🏫 第二导师的…`**（点评/逻辑拆解/升维改写对照表/交接仪式，遍布全文，如 `#L439`、`#L811`、`#L883`、`#L1114`、`#L1854` 等）。
- **`# 📄 2.2节 论文写作骨架（一页纸指南）`（`#L372`）**、**`# 🗺️ 第二章…全景回顾与完结审查`（`#L1734`）** 等元写作段落。
- **文献检索指南、翻译矩阵、SOP、避坑指南**（`#L428`、`#L723`、`#L2139` 等）——属写作方法论，非论文正文。

这些内容**对 pass2 无害（只要不把初稿当加工主体）**，但若要把初稿"洗"成干净论文需删除约一半篇幅。**建议：不清洗初稿，保留为思路存档；pass2 一律在 paper 实体上做。**

### 4. 初稿正文 vs paper 实体的内容一致性（抽样核对）

| 主题 | 初稿 | paper 实体 | 一致性 |
|---|---|---|---|
| 第一章绪论 | `#L109-L340`（完整，40 篇参考文献） | [01_background](file:///Users/bytedance/coding/AUV-Master-Project/docs/thesis/paper/01_background_and_significance.md) | **实体更精炼**；初稿含 40 篇文献列表（旧编号 [1]-[40]），实体已转 `\cite{}`。核心论点（双碳/DNV 80%/三相抵消/铠装螺旋漏磁/UA-MPC/行为树）一致。 |
| 2.3 降维反演定理 | `#L1539` FWHM 半高深度反演（核心创新） | [02 §2.3.5](file:///Users/bytedance/coding/AUV-Master-Project/docs/thesis/paper/02_system_design.md#L194) | 一致，实体保留 2.3.5 定理。 |
| 3.1.2 组合导航框架图 | `#L1826` ASCII 框图（DR+FWHM+自适应EKF+P输出） | [03 §3.1.2](file:///Users/bytedance/coding/AUV-Master-Project/docs/thesis/paper/03_state_estimation.md#L15) | 思路一致，**但初稿仅 ASCII 图、实体也无正式架构图** → 见第三部分定界。 |
| 3.2.2 锁相放大 DLIA | `#L1974` 完整 DLIA 推导 | [03 §3.2.3](file:///Users/bytedance/coding/AUV-Master-Project/docs/thesis/paper/03_state_estimation.md#L74) 工频提取 | 一致（A10 已入 `song2020dlia`）。 |

**结论**：初稿正文与 paper 实体在**核心学术论点上一致**，实体是初稿的"提炼+去水化+substantial 扩展"版本。**没有发现学术结论层面的前后矛盾**，矛盾集中在**组织结构（章节划分）与元写作残留**。

### 5. 初稿参考文献状态

初稿 `#L239-#L319` 的 [1]-[40] 旧编号列表 + `#L342` 的 `## References`（8 条网页链接）**均为旧格式**，已在 pass1 被 references.bib（75 条）+ 五章 `\cite{}` 取代。**初稿的文献列表不必再维护**（存档即可），真源是 `references.bib`。

---

## 第二部分：架构图与 drawio 清点、路径维护、一致性审核（Task #2）

### 1. 资产清点：15 套四件套（非 AGENTS 所记的 10 套）

目录 [docs/thesis/figures/architecture/](file:///Users/bytedance/coding/AUV-Master-Project/docs/thesis/figures/architecture) 下共 **15 个 `.drawio`**，每个配齐 `.png / .svg / .drawio.png` 四件套（无缺件）。另有生成脚本 `generate_architecture_diagrams.py`、预览 `preview_all.html`/`preview_v2.html`。

> ⚠️ **AGENTS_macwin §3.5 记"现有 10 张系统图"、AGENTS_linux §4.2 记"多份"——均已过时，实际 15 张。** 建议后续更新两侧 AGENTS 的该数字。

### 2. 关键矛盾：paper 只用了 8 张「旧代码向」图，7 张「v2 学术抽象」图全部成孤儿

| 分组 | 图名 | 被正文引用 | 标签风格 |
|---|---|---|---|
| **旧代码向（8，全部在用）** | autonomy_functional_loop | 02 §2.1.3 / 04new §4.1 | 抽象 OK |
| | capability_map | 02 §2.2.1 / 04new §4.1 | 抽象 OK |
| | subsystem_organization | 02 §2.2.1 | 抽象 OK |
| | **code_layer_architecture** | 02 §2.2.2 | ⚠️ **含代码文件名**（`main.py`/`main_loop.py`/`apps/`/`interfaces/`…） |
| | **ros2_node_topology** | 02 §2.2.2 | ⚠️ **含节点/文件名**（`auv_bridge`/`auv_localization`/`PySide6 Console`…） |
| | runtime_dataflow | 02 §2.2.2 | 偏工程 |
| | verification_deployment_ladder | 02 §2.2.3 | 抽象 OK |
| | **safety_arbiter_deployment** | 02 §2.5.3 | ⚠️ **含 `protocol_udp_bridge`/`brain_linux` 等实现名** |
| **v2 学术抽象（7，全部孤儿）** | **v2_dual_brain_async_hardware** | ❌ 无 | ✅ 学术抽象（"感知估计 ES-EKF"/"路径生成 UA-MPC"） |
| | **v2_five_layer_functional_architecture** | ❌ 无 | ✅ 学术抽象（"数学模型与优化算法层"…） |
| | **v2_behavior_tree_illustration** | ❌ 无 | ✅ 学术抽象（Selector/Sequence/Failsafe） |
| | **v2_mission_state_machine** | ❌ 无 | ✅ 学术抽象（IDLE/PREFLIGHT/SHADOW…） |
| | **v2_emergency_transition** | ❌ 无 | ✅ 学术抽象（4 类 Guard→Arbiter→SAFE_HOLD/SURFACE/KILL） |
| | **v2_uncertainty_highway** | ❌ 无 | ✅ 学术抽象（协方差→置信度→控制调度） |
| | **v2_mission_lifecycle_flow** | ❌ 无 | ✅ 学术抽象（4 泳道全生命周期） |

**这是本次审核发现的最重要矛盾**：
1. **project_memory 明确约定"架构图采用学术抽象（v2）：用数学/逻辑块而非代码文件名"**，且"核心示意图涵盖行为树 BT、分层 FSM、应急切换（4 类守卫）、全生命周期流转图"——**这四类正是 v2 系列**。
2. **但 paper 实体当前引用的是旧代码向 8 图，其中 `code_layer`/`ros2_node_topology`/`safety_arbiter` 三张明显违反"学术抽象、非代码文件名"约定**（暴露 `main.py`、`auv_bridge`、`protocol_udp_bridge`、`brain_linux` 等实现细节，不适合硕士学位论文正文）。
3. **memory 期望的 v2 学术图（dual_brain、five_layer、behavior_tree、mission_state_machine、emergency_transition、uncertainty_highway、lifecycle）没有一张被正文引用** → 说明 substantial 更新时新画了 v2 图但**未回填到正文**，造成"画了没用、用了不该用"的双向错位。

### 3. 图与正文内容的对应矛盾（抽样）

- **02 §2.2.2「五层软件架构」正文表**（应用/接口/行为/算法/协议层）**与孤儿图 `v2_five_layer_functional_architecture` 内容完全对应**，却引用了 `code_layer_architecture`（含文件名的旧版）。→ 应换图。
- **02 §2.2.1 双脑架构正文**（Jetson Orin NX + AMD Geode/VxWorks，UDP 72B/145B）**与孤儿图 `v2_dual_brain_async_hardware` 精确对应**（图中标注"72B 上行/145B 下行"），却**正文根本没有引用任何双脑图**（只引用了 capability_map + subsystem_organization）。→ 双脑是创新点一的核心，却缺图。
- **04new 决策与控制**大量讲行为树（§4.2）、应急自救（§4.2.3）、FSM 对比，**却只引用了 capability_map + autonomy_functional_loop 两张系统级旧图，未引用 `v2_behavior_tree_illustration` / `v2_mission_state_machine` / `v2_emergency_transition`** —— 三张最贴合第四章的 v2 图全部闲置。
- **03 状态估计全章 0 张架构图**，而 §3.1.2 组合导航框架、§3.4 不确定性量化，恰好对应孤儿图 `v2_uncertainty_highway`。→ 缺图。

### 4. 路径完整性

- 四件套路径规范、无断链；paper 正文 `../figures/architecture/*.png` 相对路径**全部命中真实文件**（8/8 命中）。
- `docs/thesis/paper/_backup_orig_0806/02_system_design.md` 为备份，引用同一批旧图（可忽略）。
- 毕业设计写作文档侧的 [相关插图（架构图）/](file:///Users/bytedance/coding/AUV-Master-Project/毕业设计写作文档/相关插图（架构图）) 为空（符合"仅存 ROS 支线过渡图"的约定）。

---

## 第三部分：定界——drawio 补充与正文位置调整初步建议（Task #3，本次不做，纳入计划）

> 依据用户指令："本次不做，但纳入计划"。以下为**初步建议清单**，供后续执行时决策。所有建议均**未落地**。

### A. 架构图「换图」建议（把已画好的 v2 学术图替换正文中的旧代码向图）

| 正文位置 | 当前引用（旧） | 建议替换为（v2 孤儿图） | 理由 |
|---|---|---|---|
| [02 §2.2.1](file:///Users/bytedance/coding/AUV-Master-Project/docs/thesis/paper/02_system_design.md#L55) 架构选型 | 无双脑图 | **新增 `v2_dual_brain_async_hardware`** | 双脑=创新点一核心，正文缺图 |
| [02 §2.2.2](file:///Users/bytedance/coding/AUV-Master-Project/docs/thesis/paper/02_system_design.md#L69) 五层软件架构 | `code_layer_architecture`（含 main.py 等文件名） | **`v2_five_layer_functional_architecture`** | 消除代码文件名，符合学术抽象约定 |
| [04new §4.2](file:///Users/bytedance/coding/AUV-Master-Project/docs/thesis/paper/04new_decision_and_control.md#L39) 行为树 | 无 BT 图 | **新增 `v2_behavior_tree_illustration`** | 第四章讲 BT 却无 BT 图 |
| 04new §4.1.2 分层解耦 / §4.2 调度 | 无 FSM 图 | **新增 `v2_mission_state_machine`** | 模式切换需 FSM 图 |
| 04new §4.2.3 应急自救优先级 | 无应急图 | **新增 `v2_emergency_transition`** | 4 类守卫仲裁正好对应 |
| [03 §3.1.2](file:///Users/bytedance/coding/AUV-Master-Project/docs/thesis/paper/03_state_estimation.md#L15) 组合导航框架 / §3.4 不确定性 | 无图（仅初稿 ASCII） | **新增 `v2_uncertainty_highway`** | 第三章缺架构图；置信度流向是全篇主线 |
| 02/05 全生命周期 | 无 | 可选 **`v2_mission_lifecycle_flow`** | 若正文有生命周期段落再补 |

### B. drawio 是否需「补绘/改绘」建议（结合 01/02/03 substantial 更新的新内容）

1. **需核对 v2 图与最新正文口径是否一致**（v2 图为 7 月初产物，01/02/03 之后有 substantial 更新）：
   - `v2_dual_brain`：图中"72B 上行/145B 下行" vs 02 §2.5.1 正文的"72B 上行/145B 下行"是否仍一致（需核 02 §2.5.1 实际数值）。
   - `v2_five_layer`：图层名（系统入口/双工适配/自治行为/数学模型/协议契约）vs 02 §2.2.2 表（应用/接口/行为/算法/协议）——**措辞不同需统一**。
   - `v2_uncertainty_highway`：是否体现 03 新引入的 `cov_to_conf` 映射与 §3.4.1 R 在线调整。
2. **可能需新绘的图**（正文有内容但 v2 系列未覆盖）：
   - **图 1.1 本文技术路线图**（01 §1.5 明确"应补充图 1.1 技术路线图"，`#L217`；初稿 `#L332` 亦点名）——**当前完全缺失**，pass2 强烈建议补。
   - **2.3 磁场建模示意**（单芯 1/r → 三相抵消 → 铠装屏蔽 → 螺旋漏磁 → FWHM 反演）：目前纯公式无示意图，可考虑补一张物理机制图。
   - **2.4 杆臂/安装角标定几何图**：正文有杆臂数学建模，无几何示意。

### C. 正文「位置/前后关联」调整建议（01/02/03 substantial 更新引入内容的定界）

1. **01 §1.5 组织架构（实体）已是六章版且自洽**，无需改；但**初稿 1.5（B）的七章版与之矛盾**——初稿仅存档，不同步。
2. **02 §2.4 声呐成像几何 + 视觉标定**：位置合理，但需确认 substantial 更新后是否仍与 03 §3.3.2 声学观测模型衔接（避免重复建模）。
3. **03 §3.1.2 组合导航框架的 ASCII 框图**（初稿 `#L1826`）→ 建议 pass2 时以 `v2_uncertainty_highway` 或新绘框架图替换 ASCII。
4. **04new §4.1 引用的 capability_map/autonomy_functional_loop 是系统级图**，放在第四章开头略突兀（更像第二章内容）→ 建议评估是否移至第二章、第四章改用 BT/FSM 图。

### D. 执行前需用户拍板的决策点
1. **换图方向**：是否同意"正文全面切换到 v2 学术抽象图、旧代码向图退役（或移至附录/internals）"？（涉及 8 处正文图改动）
2. **补绘范围**：图 1.1 技术路线图是否本轮补绘（本侧 drawio 可直接产出）？
3. **旧图去留**：`code_layer`/`ros2_node_topology`/`safety_arbiter` 三张含实现名的图，是彻底退役，还是保留到 `docs/internals/`（设计原理文档）？

---

## 附：本次审核未改动任何正文/图，仅产出本报告。下一步待用户就第三部分 D 的决策点拍板后执行。
