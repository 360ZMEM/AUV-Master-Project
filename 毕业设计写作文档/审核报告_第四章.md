# 第四章审核报告（04_decision_and_control.md）

> 审核人：写作主体对话 | 日期：2026-08-06 | 依据：完整精读 04 全文（373 行）+ 图引用核验脚本
> 结论先行：**第四章整体质量高**，五层结构（约束→调度→轨迹→控制→鲁棒性）清晰，UA-MPC 六层推导（模型→代价→UA权重→参考→约束→求解器）完整、工程动机充分。主要遗留是「排版格式残留」「占位符表」「行为树/极端场景仍 n=1」。
> 图引用核验：04 章 21 处 `![]()` 引用**全部存在、零断链**（含 2 张架构图 + 19 张实验图）。
> 与 05 章的交叉一致性：**§4.5.1（三层证据）、§4.4.6（fallback 兜底）确实存在**，解除了 05 审核报告 A4 的跳转落空担忧；§4.5.1/§4.5.2 与 §5.5.2/§5.5.4/§5.7 口径一致（真口径重跑、+2.0m bug 修复、6 极端场景）。

---

## A. 逻辑断点 / 前后矛盾（需修）

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| A1 | 4.1.1 / 4.4.1 状态维度表述 | 4.1.1 称状态向量"保留 6 个变量 `[x,y,z,ψ,u,w]`"，4.4.1 又称"精简为 6 维"并把 Fossen 全量称 12 维；但 4.5.2 提到"4-DOF 简化模型"。**6 维状态 vs "4-DOF" 说法不一致**（4-DOF 通常指 surge/heave/yaw/pitch 或类似）。 | 统一自由度口径：明确"6 维状态向量（3 位置+1 航向+2 速度），对应水平面+垂向的简化动力学"，删除或校正 4.5.2 的"4-DOF"措辞。 |
| A2 | 4.2.3 vs 4.4.6 fallback 语义 | 4.2.3 表：控制器异常→"回退到 PID baseline"；4.4.6：求解失败→"回退到上一次输出或直接返回设定点"。两处"回退目标"不完全一致（PID baseline vs last-output/setpoint）。 | 澄清两级回退关系：MPC 内层先 last-output/setpoint 兜底，持续失败再由行为树升级到 PID baseline 接管；写清层级避免读者认为矛盾。 |

## B. 排版 / 格式残留（OCR/粘贴噪声，需清理）

> 第四章有多处**全角空格缩进错乱、公式块标题被截断**，疑为从其他文档粘贴/OCR 残留。这类不影响信息但显著拉低观感，属 pass 2→3 必清项。

| # | 行号附近 | 现象 |
|---|---|---|
| B1 | L196 `#（1）` 前 | 段首多余全角空格：` UA 模式下...` |
| B2 | L204 | `         z 维度，放大系数减半` 异常缩进 |
| B3 | L210 | `--------：` 悬空破折号（原应为小标题或分隔） |
| B4 | L239 | ` `AUV_MPC_MODE` 实现` 段首缺主语、异常缩进（应为"通过环境变量 `AUV_MPC_MODE` 实现"） |
| B5 | L57-59, L65, L115 等 | 中文标点后用半角/全角混用、`:` 与 `：` 混用（如 "锁定阶段:"） |

## C. 占位符 / 待补实验（缺失项）

| # | 位置 | 占位内容 | 缺失编码 | 优先级 |
|---|---|---|---|---|
| C1 | 4.3.1 | 扫描间距计算公式、不同波束宽度扫描带宽表、覆盖率统计表（占位符） | 2 | 中（公式已有文字描述 `d=0.8×min(2R_sonar,2R_mag)`，补成公式块+表即可） |
| C2 | 4.5.2 | 极端工况控制平滑度对比表（占位符，与 05 章 B4 同源） | 3 | 高（依赖 §5.7 极端场景扩 3 seed） |
| C3 | 4.2.3 | 行为树 vs FSM 仅 n=1 单次基准 | 3 | 中（多场景统计缺口，图组已有 5 张但数据 n=1） |

## D. 可读性 / 内容增强建议

| # | 位置 | 建议 |
|---|---|---|
| D1 | 4.4.3 | UA-MPC 是核心创新点，但目前只有公式 + 参数表。建议补一段"直觉解释 + 一张 conf→权重映射曲线示意图"，帮助非专业读者理解 `(1-conf)^α` 与 sigmoid 的作用（计划书要求"增加文字内容 + High-level 推演"）。 |
| D2 | 4.4.3(4) 参数表 | A2-A4 消融变体在表中列出"消融变体"列，但结果在 §5.5.8 仍"待执行"——04/05 需同步：要么补 A2-A4 结果，要么两章都标注"变体设计已定义，结果见未来工作"。 |
| D3 | 4.2.1 / 4.2.2 | 行为树只有节点表 + 文字，无树结构图。figures/architecture 已有 `auv_v2_behavior_tree_illustration`，建议在 4.2.1 显式插入（当前仅 4.1.2 插了能力图/功能闭环图）。 |

## E. 亮点（应保留，供其他章对齐）

- **工程动机链**：每个设计选择都回答"为什么"（如显式 Euler vs RK4 的求解时间预算论证、预测时域 2s 由执行器一阶惯性反推、航速下限 0.1m/s 保舵效）。这是本章最强的写作特征。
- **诚实的能力上界**：4.5.1 三层证据明确"MPC 优势区被压缩到 LOS 不擅长的不规则曲率"，不夸大 MPC。
- **分层解耦的可验证性论证**：4.1.2"各层独立验证/独立替换"与 05 章 L1-L4 实验体系呼应。

---

## F. pass 值校正（相对 dashboard 初评）

第四章整体上调：初评部分 pass 1 实为 pass 2。校正后：
- **保持 pass 1**：4.3.2（动态航速需与感知闭环补文字+图，D1）、4.4.3（核心创新缺直觉图/结果同步，D1/D2）、4.5.1（图风格待统一+第三层证据可增强）、4.5.2（占位符表+极端场景 n=1）。
- **上调 pass 2**：4.2.1/4.2.2/4.2.3（行为树设计扎实，仅缺树图与多场景统计）、4.4.1/4.4.2/4.4.4/4.4.5/4.4.6（MPC 建模主体完整）。
- **全章 pass 3 阻塞项**：B 组排版残留必须清理。

（dashboard.md 第四章表已据此同步更新。）
