# 收尾修正计划：文档验证最后两处 Edit 修复

## 背景

端到端验证流程已完成全部实验（编译、120s实验、EKF/MPC/BT三项基准测试），日志 `docs/experiment/benchmark_test_log.md` 已完整。但 `docs/user-guide/05_benchmarks.md` 有两处修正因 Edit 调用失败未成功应用。

## 当前状态

- `benchmark_test_log.md`：完整，无需改动
- `scripts/start_experiment.sh`：已修复（kill → kill -INT）
- `05_benchmarks.md`：有两处需修正

## 修正项

### 修正 1：BT 命令去掉不存在的 `--output-dir` 参数

**文件**: `docs/user-guide/05_benchmarks.md` 第 107 行

**当前内容**:
```
python tests/benchmark_bt_vs_fsm.py --output-dir results/decision/<run_name>
```

**修正为**:
```
# 注意：此脚本无 --output-dir 参数，输出路径由 get_output_dir 决定
python tests/benchmark_bt_vs_fsm.py
```

**原因**: 实验验证发现 `benchmark_bt_vs_fsm.py` 没有 argparse 接受 `--output-dir`，传入会报错。

### 修正 2：MCAP 损坏警告更新为"已修复"

**文件**: `docs/user-guide/05_benchmarks.md` 第 283-290 行

**当前内容**:
```markdown
> **⚠️ 已知问题：`--duration` 停止可能导致 MCAP 损坏**
>
> 当使用 `--duration` 参数时，脚本通过 `kill` 终止 `ros2 bag record` 进程，可能导致
> MCAP 文件未正确写入 footer，无法被离线工具解析。
>
> **规避方法**：
> 1. 不使用 `--duration`，手动 Ctrl+C 停止实验（recorder 会正常 flush）
> 2. 或使用已有的完整录制文件：`log/experiments/benchmark_120s/rosbag_mcap/rosbag_mcap_0.mcap`
```

**修正为**:
```markdown
> **✅ 已修复：`--duration` 停止导致 MCAP 损坏**
>
> 此问题已修复（`scripts/start_experiment.sh` 第 176 行 `kill` → `kill -INT` + `sleep 2`）。
> 修复前的旧录制文件可能仍然损坏，可使用 `log/experiments/benchmark_120s/rosbag_mcap/rosbag_mcap_0.mcap` 作为已知完好的替代。
```

**原因**: `start_experiment.sh` 已修复为使用 SIGINT 信号正常关闭 recorder。

## 验证

修正后重新读取文件确认内容正确。无需重新运行实验。
