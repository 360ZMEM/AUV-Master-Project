# VxWorks 未收口问题源码审计与后续调试计划（2026-07-11）

## 1. 文档目的

本文用于承接 PC104 空板实物调试阶段的未收口问题，基于 `csd_vx6.8_lastest` 的第一轮只读源码审计，重新划分后续工作：

1. 哪些问题可以在当前“PC104 空板 + Telnet + UDP/fan-out + ROS2/PySide6”硬件环境中直接继续验证。
2. 哪些问题已经进入 VxWorks 源码根因定位阶段，需要看源码、最小侵入修改、重编译/烧录后再闭环。
3. 哪些问题需要真实 DVL/IMU/推进器/舵机等传感器或执行器，不能只靠 Telnet 注入完整闭合。
4. GUI 手动同步验收放到最后，避免 GUI 操作和底层固件问题串上下文。

本轮只读审计不修改 VxWorks 源码。

## 2. 当前源码关键发现

### 2.1 `Remote_Assignment()` 会重建主推和舵面输出

文件：

- `csd_vx6.8_lastest/DataProcess.c`

关键路径：

```c
void Remote_Assignment(_To_MCUFD *temp)
{
    ...
    if(UI_Channel_Selection_Down == 0x01)
    {
        (*temp).McuFD_Motor1_Set_Speed = UI_LORA_Instruction.FromUI12_Motor_Speed1;
    }
    if(UI_Channel_Selection_Down == 0x02)
    {
        (*temp).McuFD_Motor1_Set_Speed = UI_WIFI_Instruction.FromUI12_Motor_Speed1;
    }
    ...
    if(UI_Channel_Selection_Down == 0x02)
    {
        (*temp).McuFD_LH_Set_Rud_Location =
            (u16)(LH_Ref_Location - (UI_WIFI_Instruction.FromUI12_RCD_LH_Set_Rud_Angle) * 4096/360);
    }
    ...
    sprintf(ptr, "%05d", (*temp).McuFD_Motor1_Set_Speed);
    ...
    strncpy((char *)to_MCU_buf, ptr, strlen(ptr));
}
```

含义：

- `Remote_Assignment(&Instruction_To_FMCU)` 不是“只把现有 `Instruction_To_FMCU` 打包发送”。
- 它会重新读取 `UI_WIFI_Instruction` 或 `UI_LORA_Instruction` 的 shadow 字段，并覆盖 `Instruction_To_FMCU` 内的主推、侧推和舵面字段。
- 因此，在调用 `Remote_Assignment()` 之前直接写：

```c
Instruction_To_FMCU.McuFD_Motor1_Set_Speed = 300;
```

如果没有同步写入对应 shadow 字段，随后会被 `UI_WIFI_Instruction.FromUI12_Motor_Speed1` 或 LORA 字段覆盖。

这与实物 Telnet 现象一致：

- 舵面能进入 `Instruction_To_FMCU`。
- `motor1` 最终仍为 0。

### 2.2 `Seafloor_Grounding_Arbitration()` 的 BUG-5/BUG-6 自救主推存在覆盖风险

文件：

- `csd_vx6.8_lastest/SecurityEmergencyManage.c`

关键路径：

```c
Instruction_To_FMCU.McuFD_Motor1_Set_Speed = 300;
Remote_Assignment(&Instruction_To_FMCU);
```

以及：

```c
Instruction_To_FMCU.McuFD_Motor1_Set_Speed = 350;
Remote_Assignment(&Instruction_To_FMCU);
```

源码含义：

- DVL 丢底自救和硬阈值拉起逻辑先写 `Instruction_To_FMCU.McuFD_Motor1_Set_Speed`。
- 随后调用 `Remote_Assignment()`。
- 但 `Remote_Assignment()` 会重新用 UI shadow motor 字段覆盖主推。

当前判断：

- BUG-5/BUG-6 的根因高度集中在“自救输出没有以 `Remote_Assignment()` 可保留的方式进入最终 `$MCUFD` 打包路径”。
- 需要选择修复策略：
  1. 调用 `Remote_Assignment()` 之前同步写入 UI/LORA shadow motor 字段。
  2. 新增一个不会覆盖应急主推的 emergency 打包函数。
  3. 修改 `Remote_Assignment()`，增加 emergency override 标志或保护分支。

### 2.3 BUG-4 深度保护路径源码中已有 shadow 写入痕迹，但运行期仍未闭合

文件：

- `csd_vx6.8_lastest/SecurityEmergencyManage.c`

关键路径：

```c
if(Depth_Exceed_FromUI12_Depth_Para1 >= 10)
{
    Sys_Abnorm_Inf_Judgement |= 0x00000200;
    Instruction_To_FMCU.McuFD_Motor1_Set_Speed = 300;
    Instruction_To_FMCU.McuFD_LH_Set_Rud_Location = ...;
    Instruction_To_FMCU.McuFD_RH_Set_Rud_Location = ...;

    UI_WIFI_Instruction.FromUI12_Motor_Speed1 = 300;
    UI_WIFI_Instruction.FromUI12_RCD_LH_Set_Rud_Angle = -20;
    UI_WIFI_Instruction.FromUI12_RCD_RH_Set_Rud_Angle = -20;
    UI_LORA_Instruction.FromUI12_Motor_Speed1 = 300;
    UI_LORA_Instruction.FromUI12_RCD_LH_Set_Rud_Angle = -20;
    UI_LORA_Instruction.FromUI12_RCD_RH_Set_Rud_Angle = -20;

    Remote_Assignment(&Instruction_To_FMCU);
}
```

含义：

- 当前源码已经尝试处理 `Remote_Assignment()` 覆盖问题，即在 BUG-4 路径中同步写入 shadow 字段。
- 但运行期 BUG-4 Bit9 设置/保持仍未闭合，说明问题可能不在“源码完全没有写 shadow”，而在：
  - 实机运行镜像不是当前源码版本。
  - `Depth_Exceed_FromUI12_Depth_Para1` 没有累计到阈值。
  - `Current_State.Current_Dep` 或 `UI_WIFI_Instruction.FromUI12_Depth_Para1` 被运行期覆盖。
  - `Work_Cmd_Execute(0x91)` 初始化清零了深度保护计数器。
  - `Sys_Abnorm_Inf_Judgement` 后续被其他路径清位。
  - `Remote_Assignment()` 后又被 MainCtrlTask 的 Remote/Jetson 路径再次覆盖。

### 2.4 DVL 结构偏移不能继续依赖旧脚本硬编码

文件：

- `csd_vx6.8_lastest/DataProcess.h`

结构：

```c
typedef struct
{
    u8 head_buf[3];
    int BI_X;
    int BI_Y;
    int BI_Z;
    float BI_V;
    bool BI_Valid_Flag;
    float BD_Height;
    float BD_Time;
    float BD_Check;
    ...
}_From_DVL;
```

当前脚本曾按经验使用：

- `BD_Height = DVL_Prase_Data + 0x18`
- `BD_Check = DVL_Prase_Data + 0x20`

风险：

- `head_buf[3]` 后面存在 `int`、`float`、`bool`，VxWorks 编译器可能插入对齐 padding。
- `bool` 大小也可能影响后续 `float` 对齐。
- 手算偏移不可靠。

当前 DVL execute 现象：

- 脚本能完成 Telnet 连接、suspend、execute、resume、cleanup。
- 但注入后 `BD_Check/BD_Height` 仍读回 0。
- `Sys_Abnorm` 未置 Bit11。

新的判断：

- DVL 自动化流程已基本稳定。
- 未闭合点主要是结构偏移/运行期覆盖/仲裁触发条件，而不是 Telnet execute 框架本身。

### 2.5 `EmergencyTask` 调度频率与触发阈值

文件：

- `csd_vx6.8_lastest/main.c`
- `csd_vx6.8_lastest/SecurityEmergencyManage.c`

关键路径：

```c
wdStart(wdTimer_InfoOutputCtrl, sysClkRateGet()*0.1, ...);
...
if(Emergency_Task_Interval_Num >= Emergency_Task_Period)
{
    semGive(semEmergencyTask);
    Emergency_Task_Interval_Num = 0;
}
```

`SecurityEmergencyManage.c` 注释写明 `EmergencyTask` 是 10Hz 安全仲裁。

含义：

- BUG-4 深度超限计数阈值 `>=10`，约对应 1 秒。
- BUG-5/BUG-6 DVL 丢底计数阈值 `>=20`，约对应 2 秒。
- Telnet 注入测试必须维持状态超过这些窗口，否则不能期望置位。

## 3. 未收口问题清单与当前硬件可操作性

| 编号 | 问题 | 当前状态 | 当前空板能否继续做 | 是否需要源码 |
|---|---|---|---|---|
| GUI-1 | PySide6 GUI 手动并发 SOP | 未人工验收 | 可以 | 暂不需要 |
| BUG-4 | 深度保护 Bit9 设置/保持 | 未闭合 | 可以 | 需要 |
| BUG-5 | DVL 近底/丢底自救主推被覆盖 | 未闭合 | 可以 | 需要 |
| BUG-6 | 自救拉起主推进入最终 `$MCUFD` | 未闭合 | 可以 | 需要 |
| DVL-1 | DVL Telnet execute 流程稳定性 | 流程已稳定，功能未闭合 | 可以 | 需要 |
| DVL-2 | `BD_Height/BD_Check` 偏移标定 | 未闭合 | 可以 | 需要辅助源码/offsetof |
| ROS-1 | 更长 soak | 300 秒已通过 | 可以 | 不需要 |
| ROS-2 | 断链恢复：杀 fan-out/bridge | 未专项验证 | 可以 | 不需要 |
| ROS-3 | PC104 断电/网线断开恢复 | 未专项验证 | 可以，但需人工配合 | 不需要 |
| ZENOH-1 | sudo Zenoh 连续 PC_RAW 仲裁稳定性 | 单包已通过 | 可以 | 不需要 |
| SIM-1 | `start_lin_sim.sh both` 和 ROS2 stack 长窗口仿真回归 | 未完全闭合 | 可以 | 不需要 |
| ACT-1 | 带推进器/舵机负载动作验证 | 未做 | 当前不能完整做 | 需要真实执行器 |
| SENSOR-1 | 真实 DVL/IMU/深度融合 | 未做 | 当前不能完整做 | 需要真实传感器 |

## 4. 后续推荐执行顺序

### 阶段 A：VxWorks 源码定义补全与 Telnet 标定

目标：不改源码，先把结构偏移、运行期变量和任务触发窗口标定准确。

步骤：

1. 用源码确认 `sizeof(_From_DVL)`、`offsetof(_From_DVL, BD_Height)`、`offsetof(_From_DVL, BD_Check)`。
   - 优先方案：在 VxWorks 侧增加临时只读打印函数。
   - 若暂不改源码：用 Telnet 逐偏移写入/读回浮点，但必须严格 cleanup。
2. 用 Telnet 读取：
   - `DVL_Prase_Data`
   - `Current_State.Current_Mode`
   - `Current_State.Current_Dep`
   - `UI_WIFI_Instruction.FromUI12_Depth_Para1`
   - `Depth_Exceed_FromUI12_Depth_Para1`
   - `Sys_Abnorm_Inf_Judgement`
3. 验证 BUG-4 触发链：
   - 设置 `Current_Dep > Depth_Para1`。
   - 保持超过 1 秒。
   - 观察 `Depth_Exceed_FromUI12_Depth_Para1` 是否累计。
   - 观察 `Sys_Abnorm_Inf_Judgement & 0x00000200` 是否保持。
   - 观察 `to_MCU_buf` 中主推和舵面是否进入最终帧。
4. 验证 DVL 触发链：
   - 用正确偏移写 `BD_Check` 和 `BD_Height`。
   - 保持超过 0.5/0.3/2.0 秒阈值。
   - 观察 Bit11/Bit12/Bit13 和最终 `$MCUFD`。

阶段 A 产出：

- 一个新的 DVL offset 真值表。
- BUG-4 runtime 变量变化序列。
- BUG-5/BUG-6 runtime 变量变化序列。

### 阶段 B：VxWorks 最小侵入修复设计

目标：基于阶段 A 证据，设计最小修改，不急于大改。

候选修复方向：

1. 新增 emergency-safe 打包函数。
   - 例如 `Emergency_Assignment(_To_MCUFD *temp)`。
   - 只更新时间戳、包头、包尾、`to_MCU_buf`。
   - 不从 `UI_WIFI_Instruction`/`UI_LORA_Instruction` 覆盖 motor/rudder。
   - 适合 BUG-4/5/6 的应急最终裁决输出。

2. 修改 `Remote_Assignment()`，增加 override 标志。
   - 例如全局 `Emergency_Output_Override_Flag`。
   - flag 为 true 时跳过 UI shadow 覆盖 motor/rudder。
   - 风险：影响面比新增函数更大。

3. 在所有应急路径调用 `Remote_Assignment()` 前同步写 shadow 字段。
   - BUG-4 已有类似做法。
   - BUG-5/BUG-6 可以补齐。
   - 风险：shadow 本身会被上位机新包覆盖，而且会污染 UI shadow 语义。

推荐优先级：

1. 优先考虑新增 emergency-safe 打包函数。
2. 如果工程风险不允许新增函数，再考虑应急路径同步写 shadow。
3. 不建议直接大改 `Remote_Assignment()` 作为第一步。

所有源码修改必须遵循：

- 修改前备份原文件，例如 `SecurityEmergencyManage_bak_20260711_bug56.c`。
- 源码文件保持 GB18030 编码。
- 修改块头部添加显眼 Doxygen 注释：

```c
/**
 * @brief BUG-5/BUG-6 应急输出保持修复
 * @date 2026-07-11
 * @author 清华 AUV 课题组
 * @note 避免 Remote_Assignment() 从 UI shadow 覆盖应急主推/舵面。
 */
```

### 阶段 C：构建、烧录与实物复验

目标：让源码修改进入真实运行镜像，而不是只停留在源码目录。

步骤：

1. 构建 VxWorks 工程。
2. 确认镜像时间戳/符号表与当前源码一致。
3. 烧录或加载到 PC104。
4. Telnet 确认关键符号仍可解析。
5. 重跑：
   - BUG-4 runtime probe。
   - BUG-5/BUG-6 shadow/emergency override probe。
   - DVL soft/hard/lost execute。
6. 对照 `to_MCU_buf` 最终帧，而不仅看 `Instruction_To_FMCU`。

### 阶段 D：ROS2/fan-out/Zenoh 稳定性扩展

目标：不混入 GUI，继续完成基础设施稳定性。

可直接执行：

1. ROS2 non-passive 30 分钟零执行器 soak。
2. fan-out 运行中 kill/restart。
3. ROS2 bridge 运行中 kill/restart。
4. PC raw Zenoh 心跳连续 5 分钟。
5. PC raw 停止后 degraded 包保持安全参数。
6. fan-out 日志确认：
   - `blocked=0`
   - `depth=(500,29)`
   - `bottom=(300,200)`
   - `preset=10`
   - `motor=0/0`

### 阶段 E：仿真回归补齐

目标：确认实物基础设施和 ROS2 修复不破坏仿真。

待补齐：

1. `scripts/start_lin_sim.sh both` 长窗口。
2. `scripts/start_lin_brain.sh stack` 默认 `zenoh_json` 长窗口。
3. `/auv/state/filtered`、`/cmd_vel`、`/auv/control/setpoint` 连续可观测。
4. 确认仿真链路不占用 `21/udp`、`52364/52365/52366`。

### 阶段 F：PySide6 GUI 手动同步验收

目标：最后做 GUI，不让 GUI 操作干扰底层源码问题定位。

建议 SOP：

1. 启动 fan-out passive 或 non-passive 安全 profile。
2. 启动 PySide6：

```bash
cd console_soft/auv_console_pyside6
/usr/bin/python3 main.py --config console_config.pc104_fanout.yaml
```

3. 用户手动点击：
   - `在线模式`
   - `WiFi`
   - `确认首选项`
   - 必要时 `连接 Zenoh`
   - 必要时 `请求自主`
   - 最后 `手动接管` / `ESTOP`
4. 观察：
   - fan-out 日志中的 PySide6 下行源。
   - Telnet 中 `UI_WIFI_Instruction` 是否保持安全参数。
   - ROS2 topic 是否继续可读。
   - GUI 状态显示是否与 fan-out/ROS2/Telnet 一致。

## 5. 当前建议拍板点

1. 是否同意下一步先做 VxWorks 只读 offset 标定和 BUG-4/5/6 runtime 变量序列，而不是先改源码。
2. 若确认 BUG-5/BUG-6 根因，是否优先采用“新增 emergency-safe 打包函数”。
3. 是否允许为 DVL offset 标定临时加入只读 `printf`/Telnet 辅助函数。
4. GUI 是否继续放到固件问题计划明确后再做。
5. VxWorks 修改后是否按“构建/烧录/符号确认/实物复验”完整闭环，而不是只做源码 diff。

## 6. 暂定结论

当前未收口点中，最值得优先进入源码侧的是：

1. BUG-5/BUG-6：`Remote_Assignment()` 覆盖自救主推，源码与实物现象高度一致。
2. DVL：结构偏移和运行期覆盖需要重新标定，旧偏移不应再作为真值。
3. BUG-4：源码看似已有 shadow 修复，但运行期未闭合，需要确认镜像版本、触发计数和后续覆盖路径。

GUI 手动验收、ROS2 长 soak、断链恢复、仿真长窗口都可以继续做，但它们不应替代 VxWorks 源码根因定位。
